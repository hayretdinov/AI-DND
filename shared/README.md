# Shared

Future shared TypeScript workspace for code used by both client and server.

This folder is reserved for shared types, constants, schemas, and utilities. No shared code has been created yet.
